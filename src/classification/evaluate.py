"""Runs the fine-tuned ViT checkpoint over the test split and produces the
metrics + confusion matrix + confidence-bucket reliability report.

TODO(Phase 4): implement, saving results to results/classification/summary.json.
"""


def evaluate(checkpoint_path: str, config: dict) -> dict:
    raise NotImplementedError("Implemented in Phase 4.")
